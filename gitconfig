[core]
	editor = vim
	excludesfile = ~/.gitignore_global
[include]
    path = ~/.gitconfig.d/private
[log]
	decorate = true
[credential]
  helper = cache --timeout=86400
[push]
	default = current
[safe]
    directory = *

